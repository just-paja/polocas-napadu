/** @type {import('next-i18next').UserConfig} */
module.exports = {
  i18n: {
    defaultLocale: 'default',
    //  fallbackLocale: 'cs',
    locales: ['default', 'cs', 'en'],
    localeDetection: false,
  },
}
